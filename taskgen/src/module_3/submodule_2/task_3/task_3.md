### Тема: Проверка указателя на NULL
**Сложность:** легкая

**Задание:**
Напишите корректное условие для проверки указателя `ptr` на значение `NULL`. Ответ должен содержать оператор `if` и сравнение.

**Ожидаемый вывод:** `if (ptr == NULL)`

**Ввод:** пустой