# Тема: Динамическая память

**Сложность:** средняя

**Задание:** Какая функция соответствует описанию: {description}

**Уникальными значениями становятся:** `description`, `func_name`.

seed % 4 == 0: description = "выделяет блок памяти размером size байт; возвращает указатель на начало блока или NULL при ошибке", func_name = "malloc" 

seed % 4 == 1: description = "выделяет память для массива из count элементов размером size байт каждый и инициализирует все байты нулями; возвращает указатель или NULL", func_name = "calloc" 

seed % 4 == 2: description = "изменяет размер ранее выделенного блока памяти на size байт; может переместить блок; возвращает новый указатель или NULL", func_name = "realloc" 

seed % 4 == 3: description = "освобождает ранее выделенный блок памяти, на который указывает ptr; после вызова ptr становится недействительным", func_name = "free"

**Ввод:** пустой


## Пример для seed=15:

description = `free` 

**Ввод:** пустой

**Ожидаемый вывод:**
free
