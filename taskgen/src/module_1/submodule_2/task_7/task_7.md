# Базовые типы данных

### Задание №7
- Сложность: легкая.

- Задание: определите, сколько бит занимает переменная типа `TYPE` в среде gcc x86. В ответе укажите одно число. 

- Уникальными становятся значения `TYPE` (`char`, `short`, `int`, `long` или `long long`)

- Ввод: пуст. Вывод при `TYPE = char`: `8`.